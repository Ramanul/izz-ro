Diagnosis 2026-08-18: https://www.comunaluncavita.ro/rss.xml returns HTTP 302 to https://www.ComunaLuncavita.ro/#E403 and repeats the same redirect, so curl reaches maximum redirects. The GitHub Actions/local dry-run reports all 8 items from pl_tulcea_luncavita with URL scheme not allowed and activates source quarantine R6. The issue is the live source/feed endpoint, not the new multi-provider router. Safe remediation: add tulcea_luncavita to the existing dead-source quarantine list (or remove it from the active source list) until the RSS endpoint is independently revalidated; do not weaken guard.url_ostil or convert malformed links into https by assumption.
Source inspected: https://www.comunaluncavita.ro/rss.xml (live HTTP headers via curl). Repository source: generator/local_sources.py and generator/fetch.py in https://github.com/Ramanul/izz-ro.
