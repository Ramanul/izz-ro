# Handoff: măsurarea recuperării ingest (IZZ-0317) — 12 sept 2026

> Scris 9 sep de sesiunea de migrare Free. Cine preia: orice sesiune Claude, la 12 sept sau oricând după.
> Nu cere acordul lui Alexandru pentru CITIRI — sunt read-only. Raportează cu antet R4.

## ACTUALIZARE 9 sep seara — CAUZA RADICALĂ GĂSITĂ
Deadline-ul global de fetch (`FETCH_GLOBAL_DEADLINE_S=300` în `generator/fetch.py:104`, introdus 08-19) taie ultimele ~142 surse de fiecare rulare de când valul de primării a dus numărul la 634 (09-05). Mainstream-ul (digi24, protv, zf, libertatea, hotnews, gsp, bbc...) e înghețat din 09-05 19:51 — dovezile complete în memory `ingest-colaps-IZZ-0317.md`. Fix propus: o linie, 300→900 — **dacă între timp a primit "go" și a fost comisat, măsurarea de mai jos devine verificare post-fix; dacă NU, prezentază-i lui Alexandru fix-ul înainte de orice**.

## Contextul într-un rând
Colapsul de volum (850→~50 articole/zi de la 09-05) are trei straturi; două sunt reparate (pana de pipeline, deploy-ul lent). Rămâne regimul organic de input. Diagnosticul complet: memory `ingest-colaps-IZZ-0317.md`.

## Măsurarea (read-only, ~10 comenzi)

1. Histogramă zilnică publicat, ultimele 3 zile:
```bash
git -C C:/Users/cw_26/izz-ro fetch origin main
git -C C:/Users/cw_26/izz-ro show origin/main:data/articles.json > /c/Users/cw_26/izz-ro/probe_articles.tmp.json
python -c "
import json
from collections import Counter
d=json.load(open(r'C:/Users/cw_26/izz-ro/probe_articles.tmp.json',encoding='utf-8'))
arts=d if isinstance(d,list) else d.get('articles',d)
c=Counter(a.get('published','')[:10] for a in arts)
for zi in sorted(c)[-6:]: print(zi, c[zi])
"
rm /c/Users/cw_26/izz-ro/probe_articles.tmp.json
```
2. Rulări și eșecuri pe zile: `gh run list -R Ramanul/izz-ro --workflow build.yml --limit 80 --json conclusion,createdAt` → grupează pe zi.
3. Ultima rulare: `gh run view <id> -R Ramanul/izz-ro --log | grep -aE "^pipeline.*(Buget AI:|Amanate|Respinse DEFINITIV|Fara substanta|Upgrade fallback)"`.
4. Live: `curl -s "https://izz.ro/sitemap-news.xml?cb=$(date +%s)" | grep -c "<url>"`.

## Verdictul de decizie (alege după cifre)
- Publicat revenit la ≥300/zi ȘI „Amanate" mic → regimul organic e sănătos, raportează și închide IZZ-0317 ca observat-fără-acțiune.
- Publicat <150/zi STABIL ȘI „Amanate" ≥8/rulare constant → propune `MAX_AI_CALLS_PER_RUN` 40→60 în build.yml (fișier protejat — scrie spec și cere acordul lui Alexandru, nu atinge direct).
- „Amanate" mic dar input/rulare mic (<50) constant → problema e la SURSE (moarte/blocate anti-bot): propune diagnosticul `data/feed_cache.json` (câte surse au livrat itemi în ultimele 48h, top surse moarte). Măsurarea anti-bot din 20 sept (automatizare deja armată) măsoară parțial același lucru — coordonează-le, nu dubla.
- NU recomanda relaxarea respingerii „sub 5 cuvinte peste titlu" — e regula Zero Zgomot (domeniu), nu o pârghie.

## Nu uita
- IZZ-0317 e etichetat „blokat" în registru — după măsurare, actualizează starea (acceptat/observat) prin `python tools/registru.py`.
- Fișierele probe din rădăcina repo se șterg la final.
